# 0.6 New note in Single page app diagram

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: user types note + clicks Save

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: payload ≈ { "content": "new note here", "date": "2026-02-27T..." }
    activate server
    server-->>browser: 201 Created (or redirect/empty)
    deactivate server

    Note right of browser: browser adds the note to local state (no page reload)

    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/data.json
    activate server
    server-->>browser: updated array with the new note
    deactivate server

    Note right of browser: browser re-renders notes list
